# יצירת רשימות
my_list = [1, 2, 3]                # יצירת רשימה עם האיברים 1, 2 ו-3
empty_list = []                    # יצירת רשימה ריקה

# גישה לאיברים
first_element = my_list[0]         # גישה לאיבר הראשון (אינדקס 0)
last_element = my_list[-1]         # גישה לאיבר האחרון באמצעות אינדקס שלילי

# שינוי איברים
my_list[1] = 5                     # שינוי האיבר השני לערך 5

# הוספת איברים
my_list.append(4)                  # הוספת המספר 4 לסוף הרשימה
my_list.insert(1, 6)               # הוספת המספר 6 באינדקס 1

# הסרת איברים
removed_element = my_list.pop(2)   # הסרת והחזרת האיבר באינדקס 2
my_list.remove(5)                  # הסרת המופע הראשון של המספר 5
del my_list[0]                     # מחיקת האיבר באינדקס 0

# פעולות על רשימות
length = len(my_list)              # קבלת מספר האיברים ברשימה
sorted_list = sorted(my_list)      # החזרת גרסה ממוינת של הרשימה
reversed_list = reversed(my_list)  # החזרת איטרטור לעבור על הרשימה בסדר הפוך

# חתימת רשימה
subset = my_list[1:3]              # קבלת איברים מאינדקס 1 עד 2 (לא כולל)
first_three = my_list[:3]          # קבלת שלושת האיברים הראשונים
last_two = my_list[-2:]            # קבלת שני האיברים האחרונים

# שיטות על רשימות
count = my_list.count(3)           # ספירת מספר המופעים של המספר 3 ברשימה
index = my_list.index(2)           # קבלת האינדקס של המופע הראשון של המספר 2

# חיבור ושכפול רשימות
combined_list = my_list + [7, 8]   # חיבור שני רשימות
repeated_list = my_list * 3        # שכפול הרשימה שלוש פעמים

# ניקוי והעתקת רשימות
my_list.clear()                    # הסרת כל האיברים מהרשימה
copy_of_list = my_list.copy()       # יצירת העתק של הרשימה (העתק שטח)

# הקמת רשימה באמצעות רשימת השתייה
squared_values = [x ** 2 for x in my_list]  # יצירת רשימה חדשה עם הערכים בריבוע
filtered_values = [x for x in my_list if x > 2]  # יצירת רשימה חדשה עם הערכים שגדולים מ-2

# רשימה מקוננת
nested_list = [[1, 2], [3, 4]]     # יצירת רשימה מקוננת

# מיון רשימות (במקום והחזרה של רשימה חדשה)
my_list.sort()                     # מיון הרשימה בסדר עולה
my_list.sort(reverse=True)         # מיון הרשימה בסדר יורד
sorted_copy = sorted(my_list)      # החזרת הרשימה ממוינת מבלי לשנות את המקורית
